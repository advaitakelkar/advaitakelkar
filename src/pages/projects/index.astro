---
import { getCollection } from 'astro:content';
import ProjectList from '../../components/ProjectList.astro';

const allProjects = await getCollection('projects');
---

<ProjectList projects={allProjects} title="Projects" />
