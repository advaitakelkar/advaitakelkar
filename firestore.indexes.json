{
  "comment": "No composite indexes needed. The console's only query filters and orders on a single field (t), and Firestore builds single-field indexes automatically — declaring one here is rejected as unnecessary. Add entries only if a query ever combines two fields.",
  "indexes": [],
  "fieldOverrides": []
}
