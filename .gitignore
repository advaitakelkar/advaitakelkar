node_modules/
dist/
.astro/
.env
.env.*
!.env.example
.DS_Store
.firebase/

# Use pnpm only — block npm lockfile
package-lock.json

# Project source documents (PDF/PPTX) — synced by Drive, not deployed
source-files/
